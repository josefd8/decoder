language: java
sudo: false

services:
  - docker
  
before_install:
  - docker-compose up -d

cache:
  directories:
  - $HOME/.m2

jobs:
  include:
  - stage: test
    jdk: oraclejdk8
    script: mvn clean install -Dmaven.test.skip=true

branches:
  only:
  - master
